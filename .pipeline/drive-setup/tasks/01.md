---
id: 01
title: setup subcommand plumbing — dispatch, headless mode, doctor as terminal
status: todo
attempts: 2
verify: ["bash -n drive.sh", "bash test/setup-plumbing.sh", "bash test/setup-safety.sh"]
spec-paths: ["test/setup-plumbing.sh", "test/setup-safety.sh"]
impl-paths: ["drive.sh", "README.md"]
spec-rev: a0148cdc359192b64a68aaebce7ca5d2e205bc8c
---

# C1 — plumbing

Make `drive.sh setup` a real subcommand, peer of `doctor` (arch.md §drive.sh integration):

- `drive.sh:48` — add `setup) SUBCMD=setup; shift ;;` to the subcommand `case`.
- `drive.sh:50-52` and `:69-73` — the config-not-found and required-key (`WORKDIR/BRANCH/FEATURE`)
  guards must ALSO skip for `SUBCMD=setup` (setup has no drive.config; it CREATES config).
- Add an inline `setup()` above `doctor()` that runs the enabled steps in order (each guarded by its
  `SETUP_DO_<STEP>` toggle, default 1), tracks a `setup_bad` counter for un-automatable steps, and
  TERMINATES on `doctor` when `SETUP_DO_DOCTOR=1`: final rc = `DO_DOCTOR ? (doctor_rc | setup_bad>0) :
  (setup_bad>0?1:0)` (ADR 0002 — doctor is the sole success signal).
- `drive.sh:304` — add `if [ "$SUBCMD" = "setup" ]; then setup; exit $?; fi`.
- Usage header (`drive.sh:25-29`) + a README §Setup section (impl-paths includes README.md).

Headless activation: `SETUP_YES=1` (or `--yes`/`-y`). **Scope narrowed by ADR 0004 (review-02):** the
external installer steps + interactive picker are CUT (card 05 + `test/setup-external.sh` removed). The
wizard now generates ONLY the three config artifacts (drive.defaults / target roles.yaml / pboard block)
via the `ask_*` seam + doctor terminal. Card 01 owns orchestration + the cross-cutting safety hardening
of the shared helpers (injection-safe serialization, ordered-marker + mode-preserving pboard write,
symlink-refusing target write, non-TTY/EOF refusal) + the README run-all fix.

## Freeze coverage
- **frozen** (test/setup-plumbing.sh): setup dispatched (not a config path); headless all-off exits 0;
  doctor is the terminal step (healthy → 0 + summary; bare → 1 with a `fix:` line, honest-degrade).
- **frozen** (test/setup-safety.sh, re-spec): generated defaults survive space/newline/quote values
  without executing/injecting when sourced; an unmatched pboard marker does NOT delete to EOF; rc mode
  0600 is preserved; a non-TTY run without `--yes` refuses (no write, non-zero) — a prompt error is
  not consent. Also mandates a Bash-3.2-safe prompt (no `read -e -i`).
- **review must read**: the usage header wording, README §Setup prose, and the interactive fzf entry.

## Review 01 rejection — re-spec required

See `../reviews/review-01.md`. The semantic review found that setup orchestration is incomplete
and that the current frozen surface misses safety-critical behaviors. Before card 01 is retried,
`pipeline-task` must re-freeze the whole feature and assign the missing PRD steps 1–4a.

Required coverage/fixes:

- implement preflight/remediation, source setup, skill attachment, and dashboard build/link;
- safely quote every generated `drive.defaults` value and reject invalid enumerated choices;
- preserve rc metadata and refuse/back up malformed pboard marker state;
- fail non-interactive prompt errors unless `--yes` is explicit, including macOS Bash 3.2 behavior;
- make the public dependency toggle consistent (`SETUP_DO_DEPS` vs `SETUP_DO_PREFLIGHT`).

## Review 02 rejection — second re-spec required

See `../reviews/review-02.md`. The first re-spec fixed the basic review-01 cases, but semantic
safety-sink review found malformed-order, symlink, partial-failure, runtime-install, and PTY-cancel
paths that still lose or overwrite user state. Before card 01 is retried, `pipeline-task` must
re-freeze the whole feature with the exact offending targets named in the review.

**Resolution (ADR 0004 — narrow scope, spec-rev a0148cd):** the partial-failure + runtime-install +
picker paths (findings 3/4/7) are CUT (external steps removed). Card 01 must now, on the KEPT core:
- fix finding 1 — pboard requires exactly ONE correctly-ordered marker pair; refuse mis-ordered/
  malformed layouts (frozen: `test/setup-safety.sh` mis-ordered case);
- fix finding 2 — target roles.yaml write refuses a symlinked destination, never follows it out of the
  repo (frozen: `test/setup-target.sh` symlink-escape case);
- fix finding 5 — treat ANY read-failure/EOF (incl. TTY Ctrl-D) as refuse, not default;
- finding 6 (rc write via same-dir temp + atomic rename, recoverable backup on failure) and finding 8
  (README:194 run-all lists every setup test) are **review-reads** — implement + reviewer verifies.

Required fixes include:

- validate correctly ordered pboard markers and make rc replacement failure-atomic;
- reject target destination symlinks/path escape;
- never run `npm link` after a failed build;
- refuse or safely migrate pre-existing runtime skill directories, then verify top-level links;
- treat TTY EOF/fzf cancel as cancellation rather than default consent;
- wire the interactive runtime picker and documented grok impl-only path;
- add the re-frozen safety/external suites to README's public run-all command.
