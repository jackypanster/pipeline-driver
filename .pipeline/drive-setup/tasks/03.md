---
id: 03
title: pboard marker-delimited block into the shell rc
status: review
attempts: 0
verify: ["bash -n drive.sh", "bash test/setup-pboard.sh"]
spec-paths: ["test/setup-pboard.sh"]
impl-paths: ["drive.sh"]
spec-rev: 4ac12ec905adab2da59666447e8a264245fc3cd3
---

# C3 — pboard block (step 4b)

Implement `setup_pboard_block` (arch splits step 4 into 4a build/link and 4b block — gate 4b on its own
`SETUP_DO_PBOARD` toggle, distinct from `SETUP_DO_DASHBOARD`):

- Write the `pboard()` function into `${SETUP_SHELL_RC:-$HOME/.zshrc}` inside a marker-delimited block:
  `# >>> pipeline pboard >>>` … `# <<< pipeline pboard <<<`.
- Idempotent write = delete any existing delimited block (sed range) then append the fresh block.
- Own ONLY your markers: an unmarked legacy `pboard()` elsewhere in the rc file is left untouched.

## Freeze coverage
- **frozen** (test/setup-pboard.sh): exactly one marker pair after a fresh write, with a `pboard()`
  inside; still exactly one pair after a re-run (no duplication); a pre-existing unmarked line survives.
- **review must read**: the `pboard()` body's correctness (it renders the board + opens it), and step
  4a (`npm ci && npm run build && npm link`) which a hermetic test cannot execute — review-reads.
