---
id: 04
title: target repo .pipeline/roles.yaml init (tool-agnostic)
status: review
attempts: 0
verify: ["bash -n drive.sh", "bash test/setup-target.sh"]
spec-paths: ["test/setup-target.sh"]
impl-paths: ["drive.sh"]
spec-rev: a0148cdc359192b64a68aaebce7ca5d2e205bc8c
---

# C4 — target repo init (step 6)

Implement `setup_target` (gated by `SETUP_DO_TARGET`; target path `SETUP_TARGET_REPO`, empty = skip):

- `mkdir -p $TARGET/.pipeline`; copy `$PIPELINE_REPO/roles.yaml` → `$TARGET/.pipeline/roles.yaml`,
  rewriting the impl slot value `<autonomous-coding-skill>` → `goal-driven-implementation`.
- Keep it tool-agnostic: NO runtime/LLM name on any slot binding (CONTRACT invariant).
- Overwrite policy: `.bak`-on-change, same as C2.
- Interactive mode may loop over several target repos; headless does one (`SETUP_TARGET_REPO`).
- Step 6 does NOT scaffold a per-feature `drive.config` (PRD-confirmed non-scope).

## Freeze coverage
- **frozen** (test/setup-target.sh): roles.yaml created with impl slot = `goal-driven-implementation`
  and no placeholder; no runtime/LLM name leaks onto a slot; an existing file is backed up to `.bak`.
- **review must read**: the fzf repo picker and the multi-repo loop — coder-owned, not frozen.
