## seq=99 · 2026-06-24T10:00:00Z · impl completed · by=bot
done:   This header is MALFORMED: the transition arrow (U+2192) is missing, so
        there is no to-stage. TO is load-bearing for the route table, so a missing
        arrow (or a missing to-token) on the tail entry must parse-fail rather
        than route the coordinator off a guess.
output: tasks/03.md
--- handoff ---
>>> NEXT
Run pipeline-impl on the Hermes coder profile (goal-driven-implementation), FRESH session.
<<< END
